#import "../../comm.typ": *
#show: comm.with(title: "Cover-letter", m: 0.5in)

Dear Hiring Team,

I am applying for the *Software Engineer, AI/ML Ops* role at *BlackLine*. My background is in building production backend systems, *data pipelines*, and *LLM-based systems* with *Python*, *LangGraph*, *LangChain*, *FastAPI*, *Java*, *PostgreSQL*, *Redis*, *AWS Services*, *CI/CD*, *Docker*, and *Kubernetes*, which lines up well with the technical depth and operational discipline this position requires.

At Sperse, I built a *multi-agent* CRM platform that routes 10,000+ users across 40+ specialized sub-agents with tenant-aware access control and secure tool execution. I also built *CI/CD* pipelines for AI-agent deployments in *Docker* and *Kubernetes*, adding automated testing and release gates to catch latency, accuracy, and safety regressions before production. In parallel, I created a *FastMCP* server that exposed 160+ backend actions to AI agents and built an agentic code-review pipeline with *OpenAI Agents SDK* and *Claude Code*, automating daily PR triage across 25-30 PRs/week while reducing manual reviewer load by several hours.

Earlier at Qualitest, I designed a cloud-agnostic Storage Service in *Java* and *Spring Boot* with *PostgreSQL* and *Redis* that handled file uploads, downloads, and deletions across *Google Cloud Storage*, *S3*, and *MinIO* via expiring signed URLs. I also reduced query latency and improved scalability by optimizing *PostgreSQL* with window functions, materialized views, and indexes, then orchestrated long-running jobs with *AWS Step Functions* to reduce *AWS Lambda* timeouts and improve error recovery across 100K+ weekly executions. That mix of data integration, performance tuning, and production reliability is the same foundation I would bring to BlackLine’s data pipeline and automation work.

BlackLine’s mission to inspire, power, and guide digital finance transformation, along with its values of *Think, Create, Serve*, resonates with how I work. I care about building reliable systems that reduce manual effort, create clear operational visibility, and improve auditability so teams can move faster with confidence. I would value the opportunity to contribute to this team and its mission.

Sincerely,
Osaid Khan

#cv_ending()
